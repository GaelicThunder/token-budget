# TODO

- [ ] Auto-read usage %: integrate `ccusage` (or statusline data) so the skill can check the 5h/weekly consumption itself instead of asking the user.
- [ ] Optional PreToolUse hook that hard-blocks `Agent`/`Workflow` tools in ultra level (enforcement, not just instruction).
- [ ] Persist level + cap in a state file so the mode survives context compaction reliably.
- [ ] Per-level example transcripts in `examples/`.
